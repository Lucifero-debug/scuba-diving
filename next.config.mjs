/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
    domains: ["plus.unsplash.com", "images.pexels.com","images.unsplash.com"], // 👈 Add this line
  },
};

export default nextConfig;
